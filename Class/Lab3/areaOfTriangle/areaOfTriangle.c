#include <stdio.h>

int main(){

  int h = 10;
  int b = 20;
  float area = b * h * 0.5;

  printf("When height of the triangle is %d and the base is %d ", h, b);
  printf("the area is %.2f\n", area);

  return 0;
}
